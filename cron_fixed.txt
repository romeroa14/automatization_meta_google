# Cron job para AdMetricas - Ejecutar cada minuto
# 1. Ejecutar el scheduler de Laravel
# 2. Procesar las colas automáticamente

* * * * * cd /var/www/html/admetricas/data_ia_marketing && php artisan schedule:run > /dev/null 2>&1
* * * * * cd /var/www/html/admetricas/data_ia_marketing && php artisan queue:work --timeout=300 --stop-when-empty > /dev/null 2>&1 
